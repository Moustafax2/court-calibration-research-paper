seed: 42

data:
  project_root: .
  manifest: data/annotations.jsonl
  labels_index: data/labels/labels_index.jsonl
  sport: basketball
  split: train

pose:
  template_width: 960
  template_height: 540
  template_source: medoid
  min_template_fg_ratio: 0.02
  max_samples: 250
  min_components: 120
  max_components: 260
  step: 20
  posterior_threshold: 0.6

output:
  dir: checkpoints/pose
