seed: 42

data:
  project_root: .
  manifest: data/annotations.jsonl
  sport: basketball
  split: train

pose:
  template_width: 960
  template_height: 540
  min_components: 250
  max_components: 400
  step: 25
  posterior_threshold: 0.6

output:
  dir: checkpoints/pose
